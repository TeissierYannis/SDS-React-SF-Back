{% extends 'base.html.twig' %}

{% block title %}Categoriesequence{% endblock %}

{% block menu %}
    {% import 'macro/macroMenu.html.twig' as macroMenu %}
    {{ macroMenu.menu('Abdo en acier', 8) }}

{% endblock %}

{% block body %}
    <h1>Categoriesequence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoriesequence.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categoriesequence.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ categoriesequence.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categoriesequence_index') }}">back to list</a>

    <a href="{{ path('categoriesequence_edit', {'id': categoriesequence.id}) }}">edit</a>

    {{ include('categoriesequence/_delete_form.html.twig') }}
{% endblock %}
